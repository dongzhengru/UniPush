spring:
  application:
    name: unipush-admin
  config:
    import:
      - optional:nacos:unipush-admin.yml?namespace=unipush&group=DEFAULT_GROUP
  cloud:
    nacos:
      server-addr: 192.168.31.88:8848
      config:
        namespace: unipush
        group: DEFAULT_GROUP
        file-extension: yml
