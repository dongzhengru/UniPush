spring:
  application:
    name: unipush-core
  config:
    import:
      - optional:nacos:unipush-core.yml?namespace=unipush&group=DEFAULT_GROUP
  cloud:
    nacos:
      server-addr: 192.168.31.88:8848
      config:
        namespace: unipush
        group: DEFAULT_GROUP
        file-extension: yml
