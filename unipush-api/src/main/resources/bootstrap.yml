spring:
  application:
    name: unipush-api
  config:
    import:
      - optional:nacos:unipush-api.yml?namespace=unipush&group=DEFAULT_GROUP
  cloud:
    nacos:
      server-addr: 192.168.31.88:8848
      config:
        namespace: unipush
        group: DEFAULT_GROUP
        file-extension: yml
